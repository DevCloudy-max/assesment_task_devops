---
- name: Setup myhost
  hosts: myhost
  become: true

  tasks:
    - name: Install Nginx
      apt:
        update_cache: yes
        name: nginx
        state: present

   - name: Copy all files from /home/ubuntu to /var/www/html on remote
     shell: cp -r /home/ubuntu/* /var/www/html/
        

